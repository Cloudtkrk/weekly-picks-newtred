# 週次おすすめ商品リスト (TikTok Shop クリエイター向け)

Kalodataのエクスポートから、自社クリエイター向けの週次おすすめ商品サイトを生成し、
Vercel (GitHubリポジトリ `weekly-picks-newtred` 連携) で公開するプロジェクト。

## 毎週のワークフロー (GitHub Actions・プレビュー承認式の2段階)

**ステージ1 (自動): `input/` のエクスポートを新しい3〜4ファイルに置き換えてコミット&プッシュ**
→ Actions (`.github/workflows/weekly.yml` の `preview` ジョブ) がプレビューを生成し、
`preview/` にコミットする。**本番ページ・Discordには一切触らない**。
プレビューURL: `https://<Vercelドメイン>/preview/` (売れ筋) と `/preview/challenge.html`。
プレビューには🧪バナーが付く。

**ステージ2 (手動承認): プレビューを確認してOKなら、Actionsタブ「Weekly Picks」→ Run workflow**
→ `publish` ジョブが本生成を実行:
1. `python weekly_data_v1.py --auto input/ --html --post --top-per-cat 3`
   - `--auto` がファイルを自動判別: `Kalodata_Video_*.xlsx` → 動画 (2つあれば行数が多い方が通常動画、
     少ない方がフォロワー少動画)。`Kalodata_Product_*.xlsx` ×2 は「アップロード時間」が
     全行45日以内の方が新商品、もう一方が売れ筋。判別できない場合はエラーで停止
2. 生成成功後、直下の `index.html` / `challenge.html` を更新し `archive/`・`data/` と一緒に
   bot名義でコミット (`[skip ci]`、`preview/` はこの時点で削除) → Vercelが自動デプロイ
3. Discordに1ジャンル上位3件のembedカードを投稿 (webhookは repoのSecret `DISCORD_WEBHOOK_URL`)
4. 生成が失敗した場合はDiscord投稿もサイト更新も行われず、ジョブが失敗する

Claudeセッション経由の場合: xlsxを添付してもらい `input/` を差し替えてプッシュ→プレビューURLを案内→
ユーザーのOK後に workflow_dispatch を実行 (GitHub MCPの `actions_run_trigger`) して本公開する。

### ローカル手動実行 (フォールバック)

```
python weekly_data_v1.py --auto input/ --html
```

または従来通り `--products <売れ筋> --new <新商品> --videos <動画> --videos-small <フォロワー少>` で明示指定。
目視確認後に直下の `index.html` / `challenge.html` へコピーし、`archive/`・`data/` と一緒にコミット&プッシュ

## Kalodataエクスポート条件 (ユーザー側の手作業・参考)

エクスポート枠は **月2,000件**。週450件×4週=1,800件が基本 (フォロワー少動画100件を毎週足すと550×4=2,200件で枠超過になるため、隔週にする等の調整が必要)。

| ファイル | 条件 |
|---|---|
| 商品(売れ筋) 上位150件 | 過去30日 / 販売経路=連携クリエイター / アフィリ対応✔ / 評価4.0+ / 売上500万円+ / 売上高降順 |
| 商品(新商品) 上位100件 | 過去30日 / 販売経路=連携クリエイター / **掲載30日以内** / アフィリ対応✔ / **売上50万円+** / 成長率降順 |
| 動画 上位200件 | **過去7日** / AI生成動画=いいえ / 売上高降順 / フィルタ他なし |
| 動画(フォロワー少) 上位100件 (任意) | **過去7日** / クリエイターフォロワー数の少ない層に絞り込み / 売上高降順。🔰判定の実証データ |

## ファイル構成

- `weekly_picks.py` — データ読み込み・正規化・除外/タグ判定の共通ロジック (CONFIG含む)
- `weekly_data_v1.py` — メインCLI。突合→テーブル生成→Excel/HTML/Discord出力
- `report_html.py` — Webサイト生成 (index.html=売れ筋 / challenge.html=新商品)
- `deploy.sh` — git push型デプロイ (現運用はGitHub Web UIアップロードでも可)

## 仕様・ビジネスルール (変更時はユーザーに確認)

### 除外ルール
1. 報酬率「-」(アフィリ非対応) と報酬率3%以下 (`CONFIG["min_commission_pct"]`) は必ず除外
2. `CONFIG["exclude_names"]` に載るクリエイタープロデュース商品 (現在:「上司のあさみ」)。
   同種の商品を見つけたらこのリストに追記する
3. 商品名「様/さん/ちゃん専用」は無条件除外。「専用」を含み参画10人以下も除外 (専売品)
4. 売れ筋: 参画クリエイター10人以下は除外 (`hot_min_creators`、実質専売の疑い)
5. 新商品: 参画3人以下 (`challenge_min_creators`) と成長率マイナス (`challenge_min_growth`) を除外。
   掲載30日超 (`challenge_max_listed_days`) も除外 (エクスポート条件と二重の安全網)

### カテゴリ誤り補正
- `CONFIG["category_overrides"]` に商品名キーワード(全て含む)→正しいカテゴリのルールを列挙。
  上から順に評価し最初の一致を適用 (例: アームカバー/ワイドパンツが自動車・バイクに誤分類される問題)。
  誤分類を見つけたらここに追記する

### タグ (除外せず可視化)
- 🎫キャンペーン中: 商品名にクーポン/OFF/SALE/セール/割引/福袋
- 季節 / 薬機法注意 (カテゴリ判定) / LIVE向き・動画向き (GMVチャネル構成比60%/20%) / オーガニック・広告主導 (広告視聴率ベース)

### ⭐今週売れてる (おすすめマーク)
直近7日動画エクスポートに売上が載っている商品 (=今から乗っても売れやすい)。タグとは別枠の赤バッジ

### 新指標 (クリエイター目線、Excel/Web両方に表示)
- **参画ペース** = 参画クリエイター数 ÷ 掲載月数 (人/月)。掲載月数 = (今日 − アップロード時間)/30日、最低1ヶ月。
  累積参画数は掲載期間で歪むため (1年で150人と1ヶ月で150人は別物)。掲載日欠損は「-」
- **1人あたりGMV** = 30日売上 ÷ 参画クリエイター数。高GMVでも参画1000人超なら飽和していて新規の取り分が小さい

### 表示ポリシー (5ジャンル固定構成)
- 大分類を `CONFIG["genre_map"]` で5ジャンルに集約。未定義の大分類は「その他」:
  美容(←美容・パーソナルケア/健康) / 食品(←食品・飲料) / 家電ガジェット(←家電製品/スマートフォン・エレクトロニクス) /
  キッチン・ホーム用品(←キッチン用品/ホーム用品/ホームセンター/テキスタイル・ソフトファニシング/家具) / その他
- 表示順は `CONFIG["genre_order"]` で固定: 美容→食品→家電ガジェット→キッチン・ホーム用品→その他。
  Webのフィルタチップもこの5ジャンル
- **並び順 (売れ筋、売上高順は廃止)**: ジャンル内で ⭐(直近7日動画売上あり)を最優先 → その中で「乗りやすさ」順。
  乗りやすさ = 成立率のパーセンタイルランク + 1人あたりGMVのパーセンタイルランク。
  各ジャンル上位10件に制限 (`CONFIG["per_genre_limit"]`)
- 新商品ページは件数制限なし・同じジャンル構成で成長率順のまま
- **Webカードの表示項目は3つだけ**: 30日売上 / 単価 / 報酬率 (成長率も表示しない)。
  販売件数・成立率・参画数・参画ペース・1人あたりGMV・報酬目安などの詳細指標はWebに出さず、
  Excelと `data/*.json` にのみ残す (バッジ・並び順の判定には内部で使い続ける)
- ショップ名は現状表示不可 (Kalodata商品エクスポートに列がない)。列が取れるようになったら追加予定
- 報酬率は必ず「参考値」注記を維持する (フッター)
- 商品名突合はNFKC正規化+記号除去+先頭30文字 (`norm_name`)。商品IDはエクスポートにないため

### バッジ (サンプル承認難易度の可視化。商品名の上のピル、⭐と併記可)
- 🔰 **初心者でも狙いやすい** (緑): 参画ペース≥50人/月 × 成立率≥40% × ⭐あり、
  **または** フォロワー少クリエイター動画エクスポート (`--videos-small`) に直近7日売上が載っている商品。
  セラーが広く承認している/実績の少ないクリエイターでも実際に売れている=実績なしでもサンプルが通りやすい
- 💎 **狙い目（実績者向け）** (紫): 🔰非該当 かつ 1人あたりGMV≥10万円 × 成立率≥30%。
  取り分が大きいが承認に実績・投稿数が必要
- 🎁 **自社サンプル可** (オレンジ): 自社案件リスト (`own_list.csv`) と商品ID一致 (同名別IDは
  正規化商品名でフォールバック) した商品に自動付与。`CONFIG["own_sample_brands"]` の
  ブランド名部分一致でも付与される (こちらは現在空)。🔰/💎と独立判定で併記される
- 閾値はすべてCONFIGに定数化: `easy_pace=50` / `easy_cvr=40` / `gem_gmv_per_creator=100000` / `gem_cvr=30`
- 売れ筋ページ冒頭 (タブ下) に「自分に合う商品の選び方」ガイド、フッターに各バッジの判定基準を記載

### 名称ルール
- 表示上の名称は「チャレンジ」ではなく**「新商品」** (タブ / ページタイトル / Excelシート名 / Discord出力)。
  ただし `challenge.html` のファイル名とURLは変更しない (配布済みリンクが切れるため)。内部変数名も据え置き

### 既知のデータ癖
- 画像URL: Kalodataエクスポートは `resize-webp:...jpeg` という壊れたテンプレートで出てくる。
  `fix_image_url()` が jpeg版に修正済み (webp版フォールバックあり)。**この修正を消さないこと**
- 価格「価格(¥)」列はレンジ表記あり → 単価は「平均取引金額」を使用
- 商品エクスポートの列名: 商品名称 / 商品レビュー / 報酬率 / アップロード時間 (UIと名前が違う)

## デプロイ

- リポジトリ: `Cloudtkrk/weekly-picks-newtred` (public) → Vercel自動デプロイ
- 公開URL: Vercelプロジェクトのドメイン直下 `/` (売れ筋) と `/challenge.html`、`/archive/` (過去週一覧)
- プッシュ対象は直下の `index.html` / `challenge.html` と `archive/`・`data/` 配下、
  および Actionsの入力となる `input/*.xlsx` (privateリポジトリのためコミットOK)。
  生成物の `weekly_data_*.xlsx` と `.img_cache/` はコミットしない (.gitignore済)

## 自社案件リスト (own_list.csv / 🎁おすすめ・📺ライブページ)

- 元データはGoogleスプレッドシート「統合商品リスト」
  (ID: `1F7745gKhylgqM1AOBcJWwpZ93ibAPMfa7ubPMit1YYY`、3,661件・164ショップ。
  列: No. / 出典 / 統合カテゴリー / 元カテゴリー / サブカテゴリー / ブランド・店舗名 / 商品名 /
  価格 / 最低価格 / 最高価格 / 成果報酬率(%) / アフィリエイトURL / 商品URL / 商品ID / 重複)。
  リポジトリの `own_list.csv` はそのスナップショット (Actionsが直接Googleにアクセスできないため)
- **同期方法**: ユーザーが「リスト同期して」等と言ったら、Google Driveコネクタの
  `download_file_content` (exportMimeType=text/csv) でシートを取得し、
  `own_list.csv` (列: カテゴリ/ショップ/商品名/価格/報酬率/アフィリエイトリンク/商品ID/画像/出典/ライブ/表示)
  を再生成してコミット。`read_file_content` は大きいシートで途中までしか返らないので使わないこと。
  シートの行が増えていればそのまま商品追加になる (画像は下記の規則で自動設定)
- **シートの「表示」列**: 値がある商品は各ショップの先頭に並び ⭐イチオシ ピルが付く
- **シートの「LIVE」列**: 値 (⚪︎ / 一部可 / 一部可能 など) がある商品は 📺ライブタブに載る。
  「一部」を含む値はピルが「📺 LIVEタイムセール一部可能」になる
- **商品画像**: `own_list.csv` の画像列。`product-images/<商品ID>.jpeg` がリポジトリにあれば
  それを (Vercelが静的配信)、無ければ `https://newtrend.entercommerce.co.jp/product-images/<商品ID>.jpeg`。
  Offbeat TAPリスト(xlsx)の埋め込み画像3,300枚は抽出済みで同梱。新規商品の画像は
  同じ命名で `product-images/` に置くか、自社ホストにアップロードすれば自動で表示される
- 生成時の動作 (`weekly_data_v1.py` / `report_html.py`):
  1. 売れ筋/新商品の掲載商品と**商品IDで突合** (同名・別IDの再出品は正規化商品名でフォールバック)。
     一致した商品はカードの遷移先を**アフィリエイトリンクに差し替え** + 🎁バッジ付与
  2. `recommend.html` (🎁おすすめタブ): **ショップごとに代表 `OWN_TOP_N`(=5) 件**を掲載し、
     6件以上あるショップには「このショップの全N件を見る →」リンクを出す
  3. `shops/<ハッシュ>.html`: ショップごとの全商品ページ。ファイル名はショップ名のmd5先頭10桁で、
     週次再生成でもURLが変わらない (配布済みリンクが切れない)
  4. `live.html` (📺ライブタブ): CSVの**「ライブ」列に値がある商品**だけを集めたページ。
     列が空/無い週はページもタブも生成されない。**カードの遷移先は商品ページではなく
     `timesale/<商品ID>.html`** (タイムセール設定依頼ページ) で、1クッション挟む
  4b. `timesale/<商品ID>.html`: LIVE商品ごとの設定依頼ページ。①ショーケース追加ボタン
     (アフィリエイトリンク) ②3日間単位×最大3枠のカレンダー (本日から2日間は選択不可)
     ③TikTokアカウント名 ④備考(任意)。送信は現状フロント完結の完了画面のみで、
     スプレッドシート自動格納は未実装 (Google Apps Scriptのwebhookを立てれば対応可能)
  5. アーカイブには `shops/` を複製せず、`archive/<日付>/recommend.html` から本サイトの
     `/shops/` を参照する (毎週164ページ複製するとリポジトリが肥大化するため)
  6. `own_list.csv` が無い場合はおすすめ/ライブタブ非表示・差し替えなしの従来動作
- タブ構成: 🔥売れ筋 / 🚀新商品 / 🎁おすすめ / 📺ライブ(該当時のみ) / 📅アーカイブ

## アーカイブ

- HTML生成時に `archive/<YYYY-MM-DD>/` へその週のスナップショット、`data/<YYYY-MM-DD>.json` へ
  表示テーブル全項目を自動保存する (`--no-archive` で無効化)
- `archive/index.html` (過去週一覧) も自動再生成される。アーカイブページには注意バナーが付き、
  タブの「📅 アーカイブ」リンクから遷移できる
- data JSONは「先週比」「連続ランクイン」等の将来機能の材料。2026-07-28分は配信済みHTMLからの
  シードのため一部項目 (商品名/大分類/報酬率/参画数など) のみ

## Discord配信

`--post --webhook <URL>` (または環境変数 `DISCORD_WEBHOOK_URL`) でembedカード投稿
(1ジャンルあたり `--top-per-cat` 件、既定5・Actionsでは3。⭐は商品名先頭に付与)。

**投稿の情報構成はWebカード (`report_html._card`) と必ず一致させる**:
バッジ(🔰/💎/🎁) → 商品名 → **30日売上／単価／報酬率の3項目のみ** → タグ。
販売件数・成立率・参画数・参画ペース・1人あたりGMV・報酬目安・成長率・掲載日・カテゴリ(細分類)は
Webに出さない方針なのでDiscordにも出さない (Excelと `data/*.json` にのみ残す)。
冒頭メッセージにはWebの「自分に合う商品の選び方」ガイドと同じバッジ凡例、
報酬率の参考値注記、薬機法注意の喚起を載せる。表示項目を変更するときは
`report_html._card` と `make_embed` の両方を必ず揃えて直すこと。
なおコンソール出力は運用者の目視確認用の詳細ダンプで、投稿内容とは別物。
Actionsからは repoのSecret `DISCORD_WEBHOOK_URL` 経由で投稿される。
チャンネル切り替え時は必ずテスト用チャンネルで一度確認してから本番Webhookに差し替える。

## してはいけないこと

- 報酬率・売上などの数値を推測で補完しない (欠損はそのまま「-」表示)
- 除外ルール・閾値 (売れ筋500万円等) をユーザー確認なしに変更しない
- Kalodataへの自動ログイン/スクレイピングを実装しない (エクスポートは手動運用)
